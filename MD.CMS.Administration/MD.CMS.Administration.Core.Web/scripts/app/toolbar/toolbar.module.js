(function ()
{
    'use strict';

    angular
        .module('app.toolbar', [])
        .config(['$translatePartialLoaderProvider', config]);

    /** @ngInject */
    function config($translatePartialLoaderProvider)
    {
    }
})();
